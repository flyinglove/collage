{
  "name": "collage-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo hosting the drag-and-drop page builder and the Nuxt rendering runtime.",
  "workspaces": [
    "builder",
    "renderer"
  ],
  "scripts": {
    "dev:builder": "npm --workspace builder run dev",
    "build:builder": "npm --workspace builder run build",
    "preview:builder": "npm --workspace builder run preview",
    "dev:renderer": "npm --workspace renderer run dev",
    "build:renderer": "npm --workspace renderer run build"
  }
}
